using UnityEngine;

public class CharacterSelect : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //player presses a button, cycles through player colors/profiles
        //press ready, lock in that character and remove it from selection
        //once both players are ready, show start button. Start button opens door.

    }
}
